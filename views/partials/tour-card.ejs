<article class="tour-row">
  <span class="tour-number" aria-hidden="true">0<%= number %></span>
  <div><p class="tour-label"><%= label %></p><h3><%= name %></h3><p><%= description %></p></div>
  <a class="button button-outline js-prefill" href="#contact" data-service="<%= service %>" data-destination="<%= name %>">Request a Quote</a>
</article>
